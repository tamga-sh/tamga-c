version: 2
updates:
  # The library itself has no package dependencies to update -- that is the
  # point of it. Only the workflow actions do.
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
